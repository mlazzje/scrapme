<%= simple_form_for(@website) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :website %>
    <%= f.input :owner %>
    <%= f.input :manager %>
    <%= f.input :name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
